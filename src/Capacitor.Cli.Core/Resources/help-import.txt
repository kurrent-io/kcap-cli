kcap import — Import local sessions from every detected coding agent

Discovers Claude, Codex, Cursor, Copilot, Gemini, Kiro, Pi, OpenCode, and
Antigravity sessions on this machine and imports them in a single run. Most agents
ship a per-session .jsonl transcript (OpenCode is a SQLite db):
  Claude   — ~/.claude/projects/<encoded-cwd>/<sid>.jsonl
  Codex    — ~/.codex/sessions/.../rollout-<sid>.jsonl
  Cursor   — ~/.cursor/projects/<sanitized-workspace>/agent-transcripts/<sid>/<sid>.jsonl
  Copilot  — ~/.copilot/session-state/<sid>/events.jsonl (plus the pre-GA
             history-session-state/ root; dirs without events.jsonl are skipped)
  Gemini   — ~/.gemini/tmp/<project>/chats/session-<ISO>-<shortId>.jsonl
  Kiro     — ~/.kiro/sessions/cli/<sid>.jsonl (+ <sid>.json metadata; honours KIRO_HOME)
  Pi       — ~/.pi/agent/sessions/.../<timestamp>_<sid>.jsonl (badlogic/pi-mono)
  OpenCode — ~/.local/share/opencode/opencode.db (SQLite; sessions + parts tables)
  Antigravity — ~/.gemini/antigravity/brain/<id>/.system_generated/logs/transcript_full.jsonl
  dsh      — ~/.cache/kcap/dsh/<id>.jsonl (written by the kcap Cordis plugin)
kcap walks all ten the same way.

Usage: kcap import [vendor-filters] [scope] [options]

By default every available agent is imported. The vendor filters below let you
narrow the run to a subset; passing one or more vendor filters means ONLY those
vendors run.

Vendor filters (additive — pass one or more to restrict the run):
  --claude                Import Claude transcripts
  --codex                 Import Codex rollouts
  --cursor                Import Cursor agent-session transcripts
  --copilot               Import GitHub Copilot CLI session transcripts
  --gemini                Import Gemini CLI session transcripts
  --kiro                  Import AWS Kiro CLI sessions (~/.kiro/sessions/cli)
  --pi                    Import Pi (badlogic/pi-mono) session transcripts
  --opencode              Import SST OpenCode sessions (~/.local/share/opencode/opencode.db)
  --antigravity           Import Google Antigravity conversations (~/.gemini/antigravity/brain)
  --dsh                   Import DeepSeek Harness sessions (~/.cache/kcap/dsh)

Scope (one of, required for non-interactive use):
  --all                   Import every discovered session
  --org <owner>           Import only sessions whose git-remote owner matches <owner>
  --org                   Bare: pick an org from discovered repos (interactive) and
                          remember it; reuses the remembered org on later runs
  --repo <owner/name>     Import only sessions from a specific repo. Repeatable:
                          pass it once per repo to select several
  --repo .                Import only sessions from the current cwd's repo

The org is the git-remote owner (GitHub org/user), independent of your profile
name — so `--org` works the same under GitHub and WorkOS sign-in. A bare `--org`
with no remembered org needs an interactive terminal (or pass `--org <owner>`).

Without a scope flag on an interactive terminal, an interactive picker is shown.

Options:
  --discover              Report what is on disk and exit without importing:
                          sessions and last activity per repository, how many
                          couldn't be matched to one, and totals for each --since
                          window. Needs no scope flag — it is what you run to
                          decide which one to use
  --json                  With --discover: emit the report as JSON
  --yes, -y               Skip the confirmation prompt
  --private               Mark every imported session as Only Visible to You
  --since YYYY-MM-DD      Only import sessions started on or after this date
  --cwd <path>            Filter by working directory (composes with scope)
  --session <id>          Import a specific session only (composes with scope)
  --min-lines <n>         Skip sessions shorter than n lines (default: 15)
  --skip-title            Don't generate titles for imported Claude and Codex
                          sessions. Titling shells out to your own `claude` /
                          `codex`, once per session, on your subscription — the
                          same opt-out `kcap watch` takes. Sessions are searchable
                          without a title, and the other seven agents never title
                          locally, so this changes nothing for them.
  --generate-summaries    Also generate per-session what's-done summaries
  --reimport              Force OpenCode sessions to re-import even if a local
                          ledger records them as complete (use after deleting a
                          session server-side); scope it with the vendor/--repo/
                          --cwd/--session filters. No effect on other vendors.

Re-running the same command is idempotent — a server-side watermark dedupes
already-imported lines so nothing is double-loaded.
