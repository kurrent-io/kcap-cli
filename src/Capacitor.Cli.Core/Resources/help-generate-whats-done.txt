kcap generate-whats-done — Generate what's-done summary

Usage: kcap generate-whats-done <sessionId> [--codex]

Arguments:
  sessionId               Session ID (required)

Options:
  --codex                 Use codex exec (instead of claude -p) to generate the summary
