# AI-1124 Phase B Task 1: Implementation Report

## Summary

Task 1 of the AI-1124 Phase B implementation plan was successfully completed. The daemon now has an "unattended launcher capability" abstraction, Claude's BuildArgs method emits the required flags for unattended (review-flow) launches, and both launchers declare support for unattended hosting.

## Implementation Details

### Files Modified

1. **src/Capacitor.Cli.Daemon/Services/IHostedAgentLauncher.cs**
   - Added `bool SupportsUnattended { get; }` property to the interface
   - Includes XML documentation explaining the semantic (AI-1124 reference, both shipped launchers support it)

2. **src/Capacitor.Cli.Daemon/Services/CodexLauncher.cs**
   - Implemented `public bool SupportsUnattended => true`
   - No change to existing `BuildArgs` logic for review-flow (already emits `--ask-for-approval never` + `-c mcp_servers={}`)

3. **src/Capacitor.Cli.Daemon/Services/ClaudeLauncher.cs**
   - Implemented `public bool SupportsUnattended => true`
   - Added `const string EmptyMcpConfig = """{"mcpServers":{}}""";` (AOT-safe constant)
   - Extended the `else` branch in `BuildArgs` with unattended-specific logic:
     - When `ctx.IsReviewFlow` is true, prepends:
       - `--permission-mode bypassPermissions`
       - `--strict-mcp-config`
       - `--mcp-config {"mcpServers":{}}`
     - Preserves existing effort/model/prompt logic
   - Non-review-flow launches unchanged

4. **test/Capacitor.Cli.Tests.Unit/ClaudeLauncherReviewFlowTests.cs** (new file)
   - 5 test methods covering Claude unattended behavior:
     - `Review_flow_launch_bypasses_permissions`: asserts `--permission-mode bypassPermissions` is emitted
     - `Review_flow_launch_loads_no_mcp_servers`: validates JSON structure of `--mcp-config` value
     - `Review_flow_launch_still_passes_model_and_prompt`: confirms model and prompt args still present
     - `Non_review_flow_launch_is_unchanged_no_bypass_or_strict_mcp`: non-review-flow agents unaffected
     - `Claude_launcher_supports_unattended`: asserts capability property

5. **test/Capacitor.Cli.Tests.Unit/Codex/CodexLauncherTests.cs**
   - Added `Codex_launcher_supports_unattended()` test method

6. **test/Capacitor.Cli.Tests.Unit/AgentOrchestratorVendorTests.cs**
   - Added `public bool SupportsUnattended => true` to the `SpyHostedAgentLauncher` fake

## Test Results

### Red Phase (Verification of Expected Failures)

Command: `dotnet run --project test/Capacitor.Cli.Tests.Unit/Capacitor.Cli.Tests.Unit.csproj --treenode-filter "/*/*/ClaudeLauncherReviewFlowTests/*"`

**Expected failures:** Build error — `IHostedAgentLauncher.SupportsUnattended` not found on launchers, followed by runtime assertion failures on BuildArgs review-flow logic.

### Green Phase (Verification of Success)

#### Claude-specific tests:
```
Test run summary: Passed! 
  total: 5
  failed: 0
  succeeded: 5
  duration: 331ms
```

#### Codex tests (including new assertion):
```
Test run summary: Passed!
  total: 28
  failed: 0
  succeeded: 28
  duration: 382ms
```

#### Full unit suite baseline verification:
```
Test run summary: Passed!
  total: 2062
  failed: 0
  succeeded: 2062
  duration: 1m 23s 263ms
```

Previous baseline was 2056; the 6-test increase (5 Claude + 1 Codex) is expected and correct.

## Code Quality Observations

- **Style:** File-scoped namespaces, primary constructors, 4-space indents, aligned member columns ✓
- **TUnit compliance:** All assertions `await`ed, no `Microsoft.NET.Test.Sdk` added ✓
- **AOT-safe:** EmptyMcpConfig is a constant string literal, not a JsonNode collection expression ✓
- **Comments:** Comprehensive docstrings in interface; implementation comments explain the review-flow behavior ✓
- **No dependencies:** No new NuGet packages added ✓

## Commit Metadata

**SHA:** 5255cd5  
**Subject:** feat(daemon): unattended launcher capability + Claude review-flow unattended args  
**Files changed:** 6  
**Insertions:** 111  
**Deletions:** 0 (net change: +111 lines)

## Self-Review

### Completeness
- ✓ Interface member added with clear semantics
- ✓ Both launchers implement the capability (both return `true`)
- ✓ Claude's `BuildArgs` emits all three required flags in correct order for review-flow launches
- ✓ Non-review-flow Claude launches unchanged (no regression)
- ✓ Codex behavior unchanged (existing code left exactly as-is)
- ✓ All test assertions pass
- ✓ Full suite baseline regression-tested

### YAGNI (You Aren't Gonna Need It)
- The `SupportsUnattended` property is purely declarative in Task 1. Task 2 will wire it into `AgentOrchestrator.HandleLaunchAgent` to actually enforce the constraint, so this is not dead code — it's the seam a future vendor (e.g., Gemini, AI-899) would plug into.
- The constant string literal (not a built JsonNode) is necessary for AOT compliance, as documented.

### Concerns
**None.** The implementation is clean, straightforward, and follows the exact pattern in the plan. Both launchers return `true`, signaling they can run unattended. Claude's flags match the current Claude Code CLI documentation (verified in the plan). The test coverage is thorough: permissions bypass, MCP server config, model/prompt preservation, non-review-flow regression, and the capability property itself.

## Next Steps

Task 2 will add the `UnattendedLaunchPolicy` guard and wire it into the orchestrator to reject unattended launches on vendors that don't support them (currently both do, but the guard future-proofs against Gemini or other vendors).
